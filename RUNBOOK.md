# Marc + Dylan Website Development Runbook

Internal build/tracking document. Not a marketing page, not customer-facing, not linked from any
of the three prototype sites. This is Derek's operational record of what exists, what's verified,
what needs access or approval, and what's still outstanding, across three independent business
website projects.

**Status vocabulary used throughout this document:**

| Tag | Meaning |
|---|---|
| `VERIFIED` | Actually built AND actually tested — not assumed. |
| `ACCESS REQUIRED` | Blocked on credentials, domain access, or a system this session cannot reach. |
| `CONFIRMATION REQUIRED` | Blocked on a business decision or real information only Marc or Dylan can supply. |
| `FUTURE` | Deliberately out of scope right now — production/integration work, not prototype work. |
| `FAILURE` | Something that was attempted and did not work, with the reason recorded. |

Repository: `franciscoderek7/francisco-holdings` · Branch: `claude/three-website-prototypes-95en8x`
· Last verified against commit `9ecb2e4`.

---

## PHASE 0 — WORKSTREAM INITIALIZATION

| Project | Label | Status |
|---|---|---|
| Dylan — Floor 17 | Northern Forge Blinds | `DEVELOPMENT / PROTOTYPE` |
| Dylan — Floor 18 | DEF Property Maintenance | `DEVELOPMENT / PROTOTYPE` |
| Marc | Lindsay Blinds | `DEMO / NOT AUTHORIZED FOR PRODUCTION` |

"Floor 17" / "Floor 18" are internal labels for this runbook only — they do not appear anywhere on
the public-facing prototype pages, and no tower infrastructure is implemented or referenced by the
sites themselves (see Phase 4).

<details>
<summary><strong>Phase 0 checklist</strong> (click to expand)</summary>

- [x] Repository location verified — `VERIFIED`
- [x] Existing project structure inspected — `VERIFIED`
- [x] Existing files preserved — `VERIFIED` (`CNAME` untouched; no other pre-existing files existed)
- [x] No production sites modified — `VERIFIED`
- [x] No domains required yet — `VERIFIED`
- [x] No DNS changes — `VERIFIED`
- [x] No credentials required for prototype work — `VERIFIED`
- [x] Three businesses remain logically separated — `VERIFIED` (separate directories, separate
      branding, no shared content, no cross-links between the three sites)
- [x] Dylan Floor 17 identified as Northern Forge Blinds — `VERIFIED`
- [x] Dylan Floor 18 identified as DEF Property Maintenance — `VERIFIED`
- [x] Marc identified as Lindsay Blinds — `VERIFIED`
- [ ] Marc production authorization = NOT YET GRANTED — `CONFIRMATION REQUIRED`

</details>

---

## PHASE 1 — REPOSITORY INSPECTION

| Item | Finding | Status |
|---|---|---|
| Repository | `franciscoderek7/francisco-holdings` | `VERIFIED` |
| Branch | `claude/three-website-prototypes-95en8x` (created for this work; not merged to `main`) | `VERIFIED` |
| Existing website directories (before this work) | None. Repo contained only `CNAME` at root. | `VERIFIED` |
| Existing deployment configuration | `CNAME` → `franciscoholdingsinc.com`, pre-existing, unrelated to these three prototypes, **not modified**. No `.github/workflows/`, no `vercel.json`/`netlify.toml`, no GitHub Pages build config found for this branch. | `VERIFIED` |
| Existing build tooling | None repo-wide. All three prototypes are plain static HTML/CSS/vanilla JS — no `package.json`, no bundler, no framework, in any of the three directories. | `VERIFIED` |
| Existing assets | None pre-existing. All imagery in all three prototypes is inline CSS/SVG placeholder art generated by JS, by design — no binary image assets anywhere in the repo. | `VERIFIED` |
| Existing documentation | None pre-existing. Each business directory now has its own `README.md` (build notes, run instructions, placeholders, future roadmap). | `VERIFIED` |
| Safest location for the three prototypes | Flat, top-level directories (`lindsay-blinds/`, `northern-forge-blinds/`, `def-property-maintenance/`), sibling to `CNAME`. No duplicate infrastructure created; nothing pre-existing was at risk of collision. | `VERIFIED` |

No duplicate infrastructure was created. No existing work was deleted or overwritten — the only
pre-existing file in the repository (`CNAME`) is untouched, confirmed via `git diff` against the
commit that introduced it.

---

## PHASE 2 — DYLAN / FLOOR 17 — NORTHERN FORGE BLINDS

Directory: `northern-forge-blinds/`

| Requested page | Built as | Status |
|---|---|---|
| Homepage | `index.html` | `VERIFIED` |
| Products | `products.html` (Blinds / Shades / Shutters / Motorization as anchored sections) | `VERIFIED` |
| Services | *Folded into Products by design decision* — installation/consultation guidance lives inside Products and the Consultation flow rather than its own page (normal pattern for a small local business, left as-is) | `VERIFIED` |
| About | `about.html` | `VERIFIED` |
| Gallery | `projects.html` | `VERIFIED` |
| Consultation | `consultation.html` | `VERIFIED` |
| Contact | `contact.html` | `VERIFIED` |

Feature checklist:

- [x] Responsive mobile design — `VERIFIED` (tested 390px/1280px, zero horizontal overflow)
- [x] Professional branding — `VERIFIED` (distinct dark-slate/forged-copper identity, custom
      wordmark treatment, angled section dividers)
- [x] Product presentation — `VERIFIED`
- [x] Strong CTAs — `VERIFIED` ("Book a Consultation" / "Request a Quote" throughout)
- [x] Consultation flow — `VERIFIED` (demo-only, client-side validation, success state)
- [x] AI concierge demonstration — `VERIFIED` / `DEMO` — "AI Blinds Concierge" on `consultation.html`,
      explicitly labeled "Demo AI Concierge — simulated for this prototype, not connected to a live
      AI service." Zero network calls (confirmed via grep + Playwright).
- [x] Lead-capture demonstration — `VERIFIED` / `DEMO` — visual flow diagram + concierge hand-off
      into the consultation form; nothing is transmitted or stored anywhere.
- [x] Wow-factor motion/visual pass — `VERIFIED` — scroll-reveal animations, hero entrance
      choreography, hover micro-interactions, `prefers-reduced-motion` respected site-wide.

No business facts were invented. All contact/pricing/certification fields remain explicitly
placeholder-labeled (see Phase 8).

---

## PHASE 3 — DYLAN / FLOOR 18 — DEF PROPERTY MAINTENANCE

Directory: `def-property-maintenance/`

| Requested page | Built as | Status |
|---|---|---|
| Homepage | `index.html` | `VERIFIED` |
| Services | `services.html` (all 7 categories, status-badged) | `VERIFIED` |
| About | `about.html` | `VERIFIED` |
| Service Areas | `service-area.html` | `VERIFIED` |
| Projects/Gallery | `projects.html` (concept gallery — added this session to close a gap against this phase's spec) | `VERIFIED` |
| Request a Quote | `request-service.html` (+ "Request a Quote" CTA on homepage) | `VERIFIED` |
| Contact | `contact.html` | `VERIFIED` |

Feature checklist:

- [x] Responsive mobile design — `VERIFIED` (tested 390/768/1024/1440px, zero horizontal overflow)
- [x] Professional branding — `VERIFIED` (spruce/teal + terracotta identity, reads as
      "established operations company" per brief)
- [x] Service presentation — `VERIFIED` — all 7 categories carry an honest status badge
      (`Potential Service` / `Coming Soon` / `Availability TBC`); **none is presented as active**
- [x] Strong CTAs — `VERIFIED` ("Request a Quote" and "Contact Us" both literal, prominent CTAs)
- [x] Quote/contact flow — `VERIFIED` / `DEMO`
- [x] AI concierge demonstration — `VERIFIED` / `DEMO` — "AI Concierge" on `index.html` and
      `request-service.html`. If a visitor selects a "Coming Soon" category, the concierge says so
      honestly rather than implying availability.
- [x] Lead-capture demonstration — `VERIFIED` / `DEMO` — flow diagram + concierge hand-off into the
      intake form via same-site query params, nothing persisted.
- [x] Wow-factor motion/visual pass — `VERIFIED` — scroll-reveal (including a staggered build of the
      7-step customer journey diagram), hero entrance, badge pulse, `prefers-reduced-motion` respected.

No service was ever implied active. No business facts were invented.

---

## PHASE 4 — TOWER REPRESENTATION

| Floor | Business | Status |
|---|---|---|
| 17 | Northern Forge Blinds | `PROTOTYPE / DEVELOPMENT` |
| 18 | DEF Property Maintenance | `PROTOTYPE / DEVELOPMENT` |

This representation is conceptual and exists **only in this runbook**, for Derek's internal
tracking. It is explicitly not implemented, referenced, or hinted at on either live prototype site.

- [x] No public production deployment implied — `VERIFIED`
- [x] No active domain implied — `VERIFIED`
- [x] No customer access implied — `VERIFIED`
- [x] No completed business integration implied — `VERIFIED`
- [x] No domain required at this stage — `VERIFIED`
- [x] No private tower infrastructure exposed or referenced on either site — `VERIFIED`

---

## PHASE 5 — MARC / LINDSAY BLINDS

Directory: `lindsay-blinds/` — a completely separate prototype, sharing no code, content, or
branding with the Dylan properties.

| Requested page | Built as | Status |
|---|---|---|
| Homepage | `index.html` | `VERIFIED` |
| Products | `products.html` (Blinds / Shades / Shutters / Motorization / Repair-Service / "help me choose") | `VERIFIED` |
| Services | *Folded into Products by design decision* (Repair/Service and consultation-help are categories on the Products page rather than a separate page — normal pattern for a small local business, left as-is rather than adding a redundant page) | `VERIFIED` |
| About | `about.html` | `VERIFIED` |
| Gallery | `gallery.html` | `VERIFIED` |
| Consultation | `consultation.html` | `VERIFIED` |
| Contact | `contact.html` (added this session — contact details + demo message form) | `VERIFIED` |
| AI consultation demonstration | "AI Concierge" on `consultation.html` | `VERIFIED` / `DEMO` |
| Responsive design | Tested 390px/1440px | `VERIFIED` |
| Strong conversion flow | Free-consultation CTA + Call-Marc CTA present on every page | `VERIFIED` |

**The one verified real-world fact used anywhere on this site: phone `705-808-3022` ("Call Marc").**
Everything else (email, address, exact service-area boundary, credentials, pricing) is explicitly
placeholder-labeled — nothing was invented.

### CRITICAL LINDSAY BLINDS RULE

| Item | Status |
|---|---|
| LindsayBlinds.com deployed, modified, migrated, or overwritten | Not performed — `VERIFIED` |
| DNS or hosting changed | Not performed — `VERIFIED` |
| Production systems or private databases accessed | Not performed — `VERIFIED` |
| Customer communications sent | Not performed — `VERIFIED` |
| Production changes of any kind | Not performed — `VERIFIED` |
| Production authorization | `NOT GRANTED` |
| Credentials | `NOT PROVIDED` |
| Overall status | `CONFIRMATION REQUIRED` |

**Attempted-but-blocked item:** an earlier instruction in this engagement asked for an audit of the
live `LindsayBlinds.com` site before building the redesign concept. That audit was attempted via
this session's web-fetch tooling and failed — `FAILURE`: network egress to `lindsayblinds.com` is
blocked by this session's proxy policy (a 403 on the CONNECT tunnel, confirmed via the proxy status
endpoint, not a transient error). No workaround was attempted, per the rule against modifying or
even routing around access to the production property. The Lindsay Blinds prototype was built
using only the one verified fact (the phone number) rather than any assumed knowledge of the real
site's current content.

---

## PHASE 6 — AI EXPERIENCE

| Business | Widget name | Flow | Status |
|---|---|---|---|
| Northern Forge | AI Blinds Concierge | Visitor → Questions → Product Direction → Consultation → Lead | `VERIFIED` / `DEMO` |
| DEF | AI Concierge | Visitor → Questions → Service Direction → Quote → Lead | `VERIFIED` / `DEMO` |
| Lindsay Blinds | AI Concierge (consultation page) | Visitor → Questions → Product Direction → Consultation → Lead | `VERIFIED` / `DEMO` |

All three are polished, scripted decision-tree simulations — no live AI API is configured or
connected anywhere in this repository. Every widget carries an explicit, visible "Demo AI Concierge
— simulated for this prototype, not connected to a live AI service" label, and none of the three
ever quotes prices, promises dates, confirms availability, or impersonates the business owner.
Verified via repo-wide grep for `fetch`/`XMLHttpRequest`/`WebSocket`/external `http(s)://` calls —
zero hits outside of comments describing their absence.

A real AI backend for any of the three is `FUTURE` work — see Phase 8/11 for requirements.

---

## PHASE 7 — LEAD ARCHITECTURE

Each business has its own independent, conceptual lead flow. None connect to a real database or
external service; none share a codebase, form handler, or storage mechanism with each other.

```
VISITOR
  ↓
AI ASSISTANT (demo)
  ↓
NEEDS ASSESSMENT
  ↓
SERVICE / PRODUCT
  ↓
CONSULTATION OR QUOTE
  ↓
CONTACT INFORMATION
  ↓
LEAD  →  (ends here in the prototype — nothing is transmitted or persisted)
```

| Business | Where this is shown | Status |
|---|---|---|
| Lindsay Blinds | Visual diagram + wizard on `consultation.html` | `VERIFIED` / `DEMO` |
| Northern Forge Blinds | Visual diagram + wizard on `consultation.html` | `VERIFIED` / `DEMO` |
| DEF Property Maintenance | Visual diagram + wizard on `index.html`/`request-service.html` | `VERIFIED` / `DEMO` |

A real lead database, CRM integration, or notification pipeline for any of the three is `FUTURE`
work (see Phase 11) and requires explicit authorization before any credentials or connections are
introduced.

---

## PHASE 8 — BUSINESS INFORMATION GAPS

Nothing below is fabricated. Every row is either confirmed (from earlier verified input) or an
open item. All open items are `CONFIRMATION REQUIRED`.

<details>
<summary><strong>Lindsay Blinds</strong> (click to expand)</summary>

| Item | Status |
|---|---|
| Phone | `VERIFIED` — 705-808-3022 |
| Email | `CONFIRMATION REQUIRED` |
| Address | `CONFIRMATION REQUIRED` |
| Service area | `CONFIRMATION REQUIRED` — only "Kawartha Lakes area" is used; exact boundary unconfirmed |
| Hours | `CONFIRMATION REQUIRED` |
| Services | `CONFIRMATION REQUIRED` — repair/service scope beyond category name unconfirmed |
| Products | `CONFIRMATION REQUIRED` — specific brands/materials/lines unconfirmed |
| Pricing | `CONFIRMATION REQUIRED` |
| Photos | `CONFIRMATION REQUIRED` — gallery and headshot are placeholder art |
| Logo | `CONFIRMATION REQUIRED` — current site uses a text/initials mark only |
| Branding assets | `CONFIRMATION REQUIRED` |
| Social accounts | `CONFIRMATION REQUIRED` |
| Consultation process | `CONFIRMATION REQUIRED` — prototype process is Derek's proposed structure, not confirmed by Marc |
| Quote process | `CONFIRMATION REQUIRED` |

</details>

<details>
<summary><strong>Northern Forge Blinds</strong> (click to expand)</summary>

| Item | Status |
|---|---|
| Phone | `CONFIRMATION REQUIRED` |
| Email | `CONFIRMATION REQUIRED` |
| Address | `CONFIRMATION REQUIRED` |
| Service area | `CONFIRMATION REQUIRED` |
| Hours | `CONFIRMATION REQUIRED` |
| Services | `CONFIRMATION REQUIRED` |
| Products | `CONFIRMATION REQUIRED` |
| Pricing | `CONFIRMATION REQUIRED` |
| Photos | `CONFIRMATION REQUIRED` — all project imagery is placeholder art |
| Logo | `CONFIRMATION REQUIRED` — current site uses a custom SVG wordmark treatment, not a real logo |
| Branding assets | `CONFIRMATION REQUIRED` |
| Social accounts | `CONFIRMATION REQUIRED` |
| Consultation process | `CONFIRMATION REQUIRED` |
| Quote process | `CONFIRMATION REQUIRED` |

</details>

<details>
<summary><strong>DEF Property Maintenance</strong> (click to expand)</summary>

| Item | Status |
|---|---|
| Phone | `CONFIRMATION REQUIRED` |
| Email | `CONFIRMATION REQUIRED` |
| Address | `CONFIRMATION REQUIRED` |
| Service area | `CONFIRMATION REQUIRED` |
| Hours | `CONFIRMATION REQUIRED` |
| Services | `CONFIRMATION REQUIRED` — **which of the 7 proposed categories are real is the single biggest open question** |
| Products | N/A (service business) |
| Pricing | `CONFIRMATION REQUIRED` |
| Photos | `CONFIRMATION REQUIRED` — concept gallery is illustrative only, not real project photos |
| Logo | `CONFIRMATION REQUIRED` |
| Branding assets | `CONFIRMATION REQUIRED` |
| Social accounts | `CONFIRMATION REQUIRED` |
| Consultation process | N/A |
| Quote process | `CONFIRMATION REQUIRED` |

</details>

---

## PHASE 9 — TESTING

All results below reflect an actual Playwright/Chromium automated pass plus manual interaction
checks performed this session — nothing is marked `VERIFIED` without having been run.

| Check | Lindsay Blinds | Northern Forge Blinds | DEF Property Maintenance |
|---|---|---|---|
| Desktop layout (1440px) | `VERIFIED` | `VERIFIED` | `VERIFIED` |
| Mobile layout (390px) | `VERIFIED` | `VERIFIED` | `VERIFIED` |
| Navigation (all links resolve, 200) | `VERIFIED` | `VERIFIED` | `VERIFIED` |
| Mobile nav open/close | `VERIFIED` — one real bug found and fixed (toggle was covered by the open panel; z-index fix applied and re-verified) | `VERIFIED` | `VERIFIED` |
| Buttons / CTAs | `VERIFIED` | `VERIFIED` | `VERIFIED` |
| Forms (validation + demo success state) | `VERIFIED` | `VERIFIED` | `VERIFIED` |
| AI demo widget | `VERIFIED` | `VERIFIED` | `VERIFIED` |
| Consultation flow | `VERIFIED` | `VERIFIED` | N/A |
| Quote flow | N/A | N/A | `VERIFIED` |
| Contact flow | `VERIFIED` (standalone page added this session) | `VERIFIED` | `VERIFIED` |
| Broken links | None found — `VERIFIED` | None found — `VERIFIED` | None found — `VERIFIED` |
| Missing assets | None — all imagery is intentional placeholder art, no 404s beyond the default browser favicon request | Same | Same |
| Console errors | Zero (beyond the default missing-favicon 404, which is cosmetic) — `VERIFIED` | Zero — `VERIFIED` | Zero — `VERIFIED` |
| Build errors | N/A — no build step exists — `VERIFIED` | N/A | N/A |
| Accessibility basics (focus-visible, keyboard nav, semantic landmarks, `prefers-reduced-motion`) | `VERIFIED` | `VERIFIED` | `VERIFIED` |
| Horizontal overflow at mobile width | `VERIFIED` — two real bugs found and fixed this session (nav-panel scrollWidth inflation; a `white-space: nowrap` badge on DEF's property-care page) | `VERIFIED` | `VERIFIED` |

**Known testing limitation:** automated full-page screenshots taken without scrolling initially
showed the new scroll-reveal sections as blank on some pages. This was investigated and confirmed
to be a screenshot-timing artifact (`IntersectionObserver` never fires for off-screen content in a
non-scrolling headless capture) rather than a real bug — re-verified with `prefers-reduced-motion`
emulation, which shows all content immediately. Recorded here rather than silently discarded.

---

## PHASE 10 — PRESENTATION READY

| Business | Presentation label | Ready to demo | Status |
|---|---|---|---|
| Northern Forge Blinds | "Floor 17 — Northern Forge Blinds" (internal label only) | Yes, on phone or desktop | `VERIFIED` |
| DEF Property Maintenance | "Floor 18 — DEF Property Maintenance" (internal label only) | Yes, on phone or desktop | `VERIFIED` |
| Lindsay Blinds | "Lindsay Blinds — AI Website Concept" | Yes, on phone or desktop | `VERIFIED` — every page carries a visible "Prototype / Demo" banner so it cannot be mistaken for the live site |

All three are runnable locally with zero setup (see each business's own README for exact commands).

---

## PHASE 11 — FUTURE PRODUCTION

Nothing in this phase has been performed. All items are `FUTURE`.

**Dylan (Northern Forge Blinds + DEF Property Maintenance):**

- [ ] Domains — `FUTURE`
- [ ] Production hosting — `FUTURE`
- [ ] DNS — `FUTURE`
- [ ] Business email — `FUTURE`
- [ ] Real AI API — `FUTURE`
- [ ] Production lead database — `FUTURE`
- [ ] Analytics — `FUTURE`
- [ ] SEO — `FUTURE`
- [ ] CRM integration — `FUTURE`
- [ ] Appointment scheduling — `FUTURE`
- [ ] Notifications (email/SMS) — `FUTURE`
- [ ] Production deployment — `FUTURE`

**Marc (Lindsay Blinds)** — same future architecture, gated behind all six of the following, **in
order**, none of which have happened yet:

1. [ ] Marc approves the concept — `CONFIRMATION REQUIRED`
2. [ ] Marc authorizes Derek to proceed — `CONFIRMATION REQUIRED`
3. [ ] Necessary credentials/access supplied — `ACCESS REQUIRED`
4. [ ] Production architecture agreed upon — `CONFIRMATION REQUIRED`
5. [ ] Final content/business information supplied — `CONFIRMATION REQUIRED`
6. [ ] Deployment explicitly authorized — `CONFIRMATION REQUIRED`

---

## FINAL REPORT

### VERIFIED
- All three prototypes render correctly, are fully navigable, and pass an automated responsive/
  accessibility/console-error pass at mobile and desktop widths.
- Two real bugs were found by testing and fixed: a mobile-nav toggle that became unclickable once
  its own panel opened (Lindsay Blinds), and a badge/text overflow on mobile (DEF Property
  Maintenance property-care page). Both re-verified fixed.
- No production site, domain, DNS, or credential was touched, requested, or required.

### BUILT
- **Lindsay Blinds** (`lindsay-blinds/`): 6 pages (including the Contact page added this session),
  AI Concierge, 4-step consultation wizard, gallery, lead-flow diagram, traditional-vs-AI contrast
  section, scroll/motion polish.
- **Northern Forge Blinds** (`northern-forge-blinds/`): 6 pages, AI Blinds Concierge, consultation
  form, projects gallery, lead-flow diagram, contrast section, scroll/motion polish.
- **DEF Property Maintenance** (`def-property-maintenance/`): 9 pages (including the concept gallery
  added this session), AI Concierge, request-a-quote flow, 7-category status-badged service
  architecture, customer-journey diagram, scroll/motion polish.
- This runbook (`RUNBOOK.md`).

### DEMO
- All three AI concierge widgets — scripted decision trees, zero network calls, explicitly labeled
  as simulated.
- All consultation/quote/contact forms — client-side validation and a success state only; nothing
  is transmitted or persisted anywhere.
- DEF's customer-journey diagram and all three sites' lead-capture diagrams are illustrative, not
  wired to any real scheduling or CRM system.

### ACCESS REQUIRED
- Any future production deployment for any of the three businesses (hosting, domain, DNS).
- Any real AI API integration (needs a server-side API key — never client-side — plus a backend
  endpoint).
- Marc's Lindsay Blinds production credentials, if and when Phase 11's Marc gate is cleared.

### CONFIRMATION REQUIRED
- All business information listed in Phase 8, for all three businesses.
- All six Phase 11 gates for Marc, none of which are cleared yet.

*(Resolved without re-asking: Northern Forge Blinds and Lindsay Blinds keep Services folded into
Products rather than a separate page, and Lindsay Blinds now has a standalone `contact.html` —
both were reasonable-default calls made this session rather than blocking on Derek.)*

### FUTURE
- Everything listed in Phase 11 for both Dylan and Marc — domains, hosting, DNS, business email,
  real AI APIs, production lead databases, analytics, SEO, CRM integration, scheduling,
  notifications, and production deployment.

### FAILURE
- The requested audit of the live `LindsayBlinds.com` production site could not be performed:
  network egress to that domain is blocked by this session's proxy policy (403 on the CONNECT
  tunnel). No workaround was attempted. If this audit is still wanted, it needs either a session
  with that domain unblocked, or the content supplied directly (screenshots, page text).

---

**Reminder, per the standing rule on this engagement:** prototype ≠ production, and demonstration
≠ authorization. Current position for all three businesses: **BUILD → TEST → DEMONSTRATE** (done).
**GET APPROVAL → COLLECT AUTHORIZED INFORMATION → THEN CONSIDER PRODUCTION** (not started for any
of the three).
